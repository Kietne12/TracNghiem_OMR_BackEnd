# Code Snippets - Chi Tiết Cài Đặt\n\n## 1. Density Check - Lõi của Sửa (Line ~1140)\n\n```javascript\n// Kiểm tra độ đen (fill percentage) của bubble\nconst density = options.skipDensityCheck ? 1.0 : this.computeContourDensity(binaryMat, contour);\n\n// Bubble THẬT: density ≥ 0.25 (ít nhất 25% pixel bên trong tô đen)\n// TEXT/KHUNG: density < 0.25 (chỉ outline hoặc ít đen)\nif (density < 0.25 && !options.skipDensityCheck) {\n    if (this.config.debug) {\n        console.log(`[DEBUG] Loại bỏ contour (text/khung?) - density=${density.toFixed(3)}, area=${area.toFixed(0)}`);\n    }\n    contour.delete();\n    continue;\n}\n\n// Giữ lại bubble thật\nbubbles.push({\n    contour,\n    rect,\n    area,\n    density,  // GHI LẠI: độ đen của bubble\n    centerX: rect.x + rect.width / 2,\n    centerY: rect.y + rect.height / 2,\n});\n```\n\n### computeContourDensity (Line ~803)\n\n```javascript\ncomputeContourDensity(binaryMat, contour) {\n    // Tạo mask: vẽ contour fill toàn bộ vùng bên trong\n    const mask = cv.Mat.zeros(binaryMat.rows, binaryMat.cols, cv.CV_8UC1);\n    const contourVec = new cv.MatVector();\n    contourVec.push_back(contour);\n    cv.drawContours(mask, contourVec, 0, new cv.Scalar(255), -1);  // -1 = fill\n\n    // AND: chỉ giữ pixel đen bên trong contour\n    const masked = new cv.Mat();\n    cv.bitwise_and(binaryMat, binaryMat, masked, mask);\n\n    // Tính tỉ lệ\n    const nonZero = cv.countNonZero(masked);        // số pixel đen bên trong\n    const area = Math.max(1, cv.countNonZero(mask)); // tổng pixel trong contour\n    const density = nonZero / area;                   // = [0.0, 1.0]\n\n    masked.delete();\n    mask.delete();\n    contourVec.delete();\n\n    return density;\n    // density = 0.8-1.0 → bubble thật (tô đen)\n    // density = 0.1-0.3 → text/khung (outline)\n}\n```\n\n---\n\n## 2. Outer Anchor Detection Cải Tiến (Line ~457)\n\n```javascript\ndetectOuterAnchors(src) {\n    // Convert → Grayscale\n    const gray = new cv.Mat();\n    if (src.channels() === 4) {\n        cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);\n    } else if (src.channels() === 3) {\n        cv.cvtColor(src, gray, cv.COLOR_BGR2GRAY);\n    }\n\n    // Binary inversion: tìm vùng đen (255 = đen, 0 = sáng)\n    const binary = new cv.Mat();\n    cv.threshold(gray, binary, 128, 255, cv.THRESH_BINARY_INV);\n\n    // Chia 4 vùng góc 25%\n    const cornerWindows = {\n        topLeft: makeRect(0, 0, w * 0.25, h * 0.25, w, h),\n        topRight: makeRect(w * 0.75, 0, w * 0.25, h * 0.25, w, h),\n        bottomLeft: makeRect(0, h * 0.75, w * 0.25, h * 0.25, w, h),\n        bottomRight: makeRect(w * 0.75, h * 0.75, w * 0.25, h * 0.25, w, h),\n    };\n\n    // Tìm 4 anchor\n    const points = [\n        this.findLargeAnchorInWindow(binary, cornerWindows.topLeft, \"outer-topLeft\", w * h),\n        this.findLargeAnchorInWindow(binary, cornerWindows.topRight, \"outer-topRight\", w * h),\n        this.findLargeAnchorInWindow(binary, cornerWindows.bottomRight, \"outer-bottomRight\", w * h),\n        this.findLargeAnchorInWindow(binary, cornerWindows.bottomLeft, \"outer-bottomLeft\", w * h),\n    ];\n\n    return orderPoints(points);  // [TL, TR, BR, BL]\n}\n\nfindLargeAnchorInWindow(binaryMat, windowRect, anchorLabel, imageArea) {\n    const roi = binaryMat.roi(new cv.Rect(windowRect.x, windowRect.y, windowRect.width, windowRect.height));\n    const contours = new cv.MatVector();\n    const hierarchy = new cv.Mat();\n\n    try {\n        cv.findContours(roi, contours, hierarchy, cv.RETR_LIST, cv.CHAIN_APPROX_SIMPLE);\n\n        // *** KỸ CÔN: Lọc kích thước LỚNN (chấm neo, không phải text) ***\n        const minAreaForAnchor = imageArea * 0.001;  // 0.1% ảnh\n        const maxAreaForAnchor = imageArea * 0.1;    // 10% ảnh\n\n        let best = null;\n        for (let i = 0; i < contours.size(); i += 1) {\n            const contour = contours.get(i);\n            const area = cv.contourArea(contour);\n\n            // Bỏ contour quá nhỏ hoặc quá lớn\n            if (area < minAreaForAnchor || area > maxAreaForAnchor) {\n                contour.delete();\n                continue;\n            }\n\n            const rect = cv.boundingRect(contour);\n            const aspect = rect.width / Math.max(1, rect.height);\n\n            // Aspect ratio phải gần tròn (0.7-1.3)\n            if (aspect < 0.7 || aspect > 1.3) {\n                contour.delete();\n                continue;\n            }\n\n            // Circularity phải cao (≥ 0.5 để phân biệt với khung)\n            const perimeter = cv.arcLength(contour, true);\n            const circularity = (4 * Math.PI * area) / (perimeter * perimeter + 1e-6);\n            if (circularity < 0.5) {\n                contour.delete();\n                continue;\n            }\n\n            const score = area - Math.abs(aspect - 1) * 10;\n            if (!best || score > best.score) {\n                if (best?.contour) best.contour.delete();\n                best = { contour, rect, score };\n            } else {\n                contour.delete();\n            }\n        }\n\n        if (!best) {\n            throw new Error(`Không tìm thấy chấm neo lớn trong vùng ${anchorLabel}`);\n        }\n\n        const x = windowRect.x + best.rect.x + best.rect.width / 2;\n        const y = windowRect.y + best.rect.y + best.rect.height / 2;\n\n        console.log(`[OUTER ANCHOR] ${anchorLabel} → (${Math.round(x)}, ${Math.round(y)}) area=${Math.round(best.rect.width)}x${Math.round(best.rect.height)}`);\n\n        best.contour.delete();\n        return { x, y };\n    } finally {\n        roi.delete();\n        contours.delete();\n        hierarchy.delete();\n    }\n}\n```\n\n---\n\n## 3. STEP 1/2/3 Log Điểm (Line ~369)\n\n```javascript\nprocessByAutoAnchorsFromMat(src) {\n    try {\n        // *** BƯỚC 1: Tìm 4 điểm neo góc ***\n        const outerAnchors = this.detectOuterAnchors(src);\n        if (this.config.debug) {\n            console.log(\n                `[STEP 1] Outer Anchors: TL(${Math.round(outerAnchors[0].x)},${Math.round(outerAnchors[0].y)}) ` +\n                `TR(${Math.round(outerAnchors[1].x)},${Math.round(outerAnchors[1].y)}) ` +\n                `BR(${Math.round(outerAnchors[2].x)},${Math.round(outerAnchors[2].y)}) ` +\n                `BL(${Math.round(outerAnchors[3].x)},${Math.round(outerAnchors[3].y)})`\n            );\n        }\n\n        // Warp trang 1200x1700\n        const standardPageSize = { width: 1200, height: 1700 };\n        pageWarped = this.warpRegionByAnchors(src, outerAnchors, standardPageSize);\n        ...\n\n        // *** BƯỚC 2: Xác định 3 vùng ***\n        const regionAnchors = this.detectRegionAnchors(pageBinary, pageWarped.cols, pageWarped.rows);\n        if (this.config.debug) {\n            console.log(\n                `[STEP 2] Region Anchors detected: ` +\n                `SBD=${regionAnchors.sbd.length} points, ` +\n                `MaDe=${regionAnchors.maDe.length} points, ` +\n                `Answers=${regionAnchors.answers.length} points`\n            );\n        }\n\n        // *** BƯỚC 3A: Crop 3 vùng ==========\n        sbdColor = this.warpRegionByAnchors(pageWarped, regionAnchors.sbd, { width: 300, height: 560 });\n        maDeColor = this.warpRegionByAnchors(pageWarped, regionAnchors.maDe, { width: 220, height: 560 });\n        answersColor = this.warpRegionByAnchors(pageWarped, regionAnchors.answers, { width: 1500, height: 1000 });\n        sbdBinary = this.preprocessToBinary(sbdColor);\n        maDeBinary = this.preprocessToBinary(maDeColor);\n        answersBinary = this.preprocessToBinary(answersColor);\n\n        // *** BƯỚC 3B: Phát hiện & lọc trong mỗi vùng ***\n        if (this.config.debug) {\n            console.log(`[STEP 3] SBD: findContours trong vùng crop ${sbdColor.cols}x${sbdColor.rows}`);\n        }\n        const sbdResult = this.detectNumericGrid(sbdBinary, { digitColumns: 5, digitRows: 10, label: \"SBD\" });\n\n        if (this.config.debug) {\n            console.log(`[STEP 3] MãĐề: findContours trong vùng crop ${maDeColor.cols}x${maDeColor.rows}`);\n        }\n        const maDeResult = this.detectNumericGrid(maDeBinary, { digitColumns: 3, digitRows: 10, label: \"MãĐề\" });\n\n        if (this.config.debug) {\n            console.log(\n                `[STEP 3] Answers: findContours trong vùng crop ${answersColor.cols}x${answersColor.rows} ` +\n                `(dùng density check để loại bỏ text/khung)`\n            );\n        }\n        const answerBubbles = this.detectBubblesWithRetry(answersBinary, answersColor.cols * answersColor.rows);\n        const answerStructured = this.sortAndGroupBubbles(answerBubbles);\n        if (this.config.debug) {\n            console.log(\n                `[STEP 3] Answers: Phát hiện ${answerBubbles.length} bubble, ` +\n                `ghép thành ${answerStructured.length} câu hỏi (kỳ vọng: 120)`\n            );\n        }\n        const answerResult = this.detectAnswers(answersBinary, answerStructured);\n    } finally {\n        // cleanup...\n    }\n}\n```\n\n---\n\n## 4. Circularity Tăng Cao\n\n```javascript\n// DEFAULT_CONFIG (Line 23-27)\nconst profiles = [\n    {\n        label: \"strict\",\n        minCircularity: 0.45,  // ← không đổi\n        ...\n    },\n    {\n        label: \"balanced\",\n        minCircularity: 0.35,  // ← TĂNG từ 0.3\n        ...\n    },\n    {\n        label: \"relaxed\",\n        minCircularity: 0.25,  // ← TĂNG từ 0.2\n        ...\n    },\n];\n```\n\n---\n\n## 5. Debug Output Ví Dụ\n\n```\n[STEP 1] Outer Anchors: TL(45,50) TR(1155,45) BR(1150,1695) BL(50,1700)\n[STEP 1] Warp trang: 1200x1700\n[STEP 2] Region Anchors detected: SBD=4 points, MaDe=4 points, Answers=4 points\n[STEP 3] SBD: findContours trong vùng crop 300x560\n[STEP 3] MãĐề: findContours trong vùng crop 220x560\n[STEP 3] Answers: findContours trong vùng crop 1500x1000 (dùng density check để loại bỏ text/khung)\n[OMR] Bubble profile=strict → 250 bubbles (với density check)\n[OMR] Bubble profile=balanced → 420 bubbles (với density check)\n[OMR] Bubble profile=relaxed → 380 bubbles (với density check)\n[OMR] Chọn profile=balanced với 420 bubbles (sau khi lọc text/khung bằng density check)\n[STEP 3] Answers: Phát hiện 420 bubble, ghép thành 120 câu hỏi (kỳ vọng: 120)\n[DEBUG] Loại bỏ contour (text/khung?) - density=0.142, area=156\n[DEBUG] Loại bỏ contour (text/khung?) - density=0.108, area=89\n[Q1] C1R1 | A:0.7812 B:0.1245 C:0.0892 D:0.0634 => A\n[Q2] C1R2 | A:0.8523 B:0.1523 C:0.0754 D:0.0654 => A\n[Q3] C1R3 | A:0.0234 B:0.0189 C:0.0154 D:0.0167 => Blank\n...\n```\n\n---\n\n## 6. Điều Chỉnh Nếu Vấn Đề\n\n### Vấn đề: Quá ít bubble\n\n**Giảm circularity threshold:**\n```javascript\nconst profiles = [\n    {\n        label: \"balanced\",\n        minCircularity: 0.30,  // giảm từ 0.35\n        ...\n    },\n];\n```\n\n### Vấn đề: Quá nhiều text detected\n\n**Tăng density threshold:**\n```javascript\n// Line ~1140 trong detectBubblesByProfile\nif (density < 0.30 && !options.skipDensityCheck) {  // tăng từ 0.25\n    contour.delete();\n    continue;\n}\n```\n\n### Vấn đề: Không tìm thấy Outer Anchor\n\n**Tăng cornerRatio:**\n```javascript\n// In detectOuterAnchors\nconst cornerRatio = 0.30;  // tăng từ 0.25\n```\n\n---\n\n**Phiên bản:** 2.0\n**Ngày:** 2026-03-22\n